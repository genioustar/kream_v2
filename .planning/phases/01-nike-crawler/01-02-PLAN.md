---
phase: 01-nike-crawler
plan: 02
type: execute
wave: 2
depends_on:
  - 01-nike-crawler-01
files_modified:
  - main.py
  - Makefile
  - ARCHITECTURE.md
  - history.md
autonomous: false
requirements:
  - SRC-02
user_setup: []

must_haves:
  truths:
    - "make full 실행 시 STEP 1 에서 나이키 크롤링이 호출되고 output/YYYYMMDD/nike_products.json 이 생성된다"
    - "나이키 크롤링이 실패해도(예외/빈결과) 네이버·아디다스 결과는 정상 저장되고 STEP 2~4 로 진행된다"
    - "오늘자 nike_products.json 이 이미 존재하면 나이키 크롤링이 스킵된다 (네이버·아디다스 동일 패턴)"
    - "STEP 2 의 _load_all_products 가 nike_products.json 을 자동 로드한다 (현재 *_products.json glob 패턴 덕분에 코드 변경 없이 동작)"
    - "diff_output.py 의 DEFAULT_PRODUCTS_KEY 가 nike_products.json 에 자동 적용된다 (코드 변경 없음)"
    - "Kream 차익 비교 결과(arbitrage_results.json)에 site_name='nike' 항목이 포함될 수 있다"
    - "make nike 명령으로 나이키만 단독 크롤링 실행 가능하다"
    - "ARCHITECTURE.md 에 nike/ 모듈 설명과 디렉토리 트리, 새 크롤링 소스 추가 방법 갱신이 반영된다"
    - "history.md 에 Phase 1 변경 요약이 한 줄로 추가된다"
  artifacts:
    - path: "main.py"
      provides: "STEP 1 에 나이키 크롤링 블록 추가"
      contains: "from nike.crawler import crawl_nike"
    - path: "Makefile"
      provides: "make nike 단독 실행 타겟 + .PHONY 갱신"
      contains: "nike:"
    - path: "ARCHITECTURE.md"
      provides: "nike/ 모듈 문서화"
      contains: "nike/"
    - path: "history.md"
      provides: "Phase 1 변경 히스토리"
      contains: "나이키"
  key_links:
    - from: "main.py"
      to: "nike/crawler.py"
      via: "from nike.crawler import crawl_nike"
      pattern: "from nike\\.crawler import crawl_nike"
    - from: "main.py STEP 1"
      to: "output/YYYYMMDD/nike_products.json"
      via: "_save_json(nike_products, nike_output)"
      pattern: "nike_products\\.json"
    - from: "main.py 나이키 블록"
      to: "예외 격리"
      via: "try/except 로 감싸 파이프라인 중단 방지"
      pattern: "try:[\\s\\S]*?await crawl_nike\\(\\)[\\s\\S]*?except"
    - from: "Makefile"
      to: "nike/crawler.py 단독 실행"
      via: "$(PYTHON) nike/crawler.py"
      pattern: "nike/crawler\\.py"
---

<objective>
01-01 에서 만든 `nike/crawler.py` 를 기존 파이프라인에 wire 한다. main.py STEP 1 에 나이키 크롤링 호출 블록을 추가하고, Makefile 에 `make nike` 타겟을 추가하며, ARCHITECTURE.md 와 history.md 를 갱신한다. 마지막으로 사람이 `make nike` 또는 `make full` 을 실행하여 실제 nike.com 응답·DOM 을 검증한다.

Purpose: SRC-02 요구사항 충족 (새 소스 NaverProduct 포맷 자동 통합). 핵심 인사이트는 **`_load_all_products` 가 `*_products.json` glob 으로 작동하고 `DEFAULT_PRODUCTS_KEY` 가 `*_products.json` 미등록 파일에 자동 적용되므로, STEP 2~4 와 diff_output.py 는 코드 변경 없이 자동으로 나이키를 처리한다**는 점이다 (ARCHITECTURE.md 의 "새 크롤링 소스 추가 방법" 섹션). 따라서 main.py 변경은 STEP 1 의 크롤링 호출 블록 1개로 끝난다.

Output:
- `main.py`: import 1줄 + STEP 1 나이키 블록 ~15줄 추가 (아디다스 블록 직후)
- `Makefile`: `make nike` 타겟 추가 + `.PHONY` 갱신
- `ARCHITECTURE.md`: nike/ 디렉토리 트리, 모듈 설명, 실행 방법, 흐름 다이어그램 갱신
- `history.md`: Phase 1 한 줄 요약 (없으면 신규 생성)
- 사람 검증 체크포인트: `make nike` 실행 후 `output/YYYYMMDD/nike_products.json` 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-nike-crawler/01-RESEARCH.md
@.planning/phases/01-nike-crawler/01-01-PLAN.md
@CLAUDE.md
@ARCHITECTURE.md

# 수정 대상
@main.py
@Makefile

# 참조 (이전 플랜 결과)
@nike/crawler.py
@config.py

# 자동 통합을 보장하는 기존 파이프라인 코드 (수정 금지)
@diff_output.py

<interfaces>
<!-- main.py 에서 사용 가능한 기존 함수/상수 -->

From main.py (이미 정의됨, 재사용):
```python
def _save_json(data: list, path: Path) -> None: ...
def _load_all_products(output_dir: Path) -> list[NaverProduct]: ...  # *_products.json glob - nike 자동 포함
```

From nike/crawler.py (Plan 01-01 결과):
```python
async def crawl_nike() -> list[NaverProduct]:
    """빈 결과나 차단 시에도 예외 없이 [] 반환. 단, 외부 예외(network 등)는 raise 가능."""
```

From diff_output.py (수정 불필요, 자동 동작):
```python
DEFAULT_PRODUCTS_KEY = ("model_name", "site_name")
# *_products.json 미등록 파일은 자동으로 DEFAULT_PRODUCTS_KEY 적용
# → nike_products.json 도 자동 처리됨
```

### 아디다스 크롤링 블록 (참조 패턴, main.py 내 기존 코드)
```python
# 아디다스 크롤링
adidas_output = output_dir / "adidas_products.json"
if adidas_output.exists():
    logger.info(f"오늘자 파일 존재 — 아디다스 크롤링 스킵: {adidas_output}")
else:
    logger.info("아디다스 Extra Sale 크롤링 시작")
    try:
        adidas_products = await crawl_adidas()
        if adidas_products:
            _save_json(adidas_products, adidas_output)
            logger.info(f"아디다스 상품 {len(adidas_products)}개 저장 → {adidas_output}")
        else:
            logger.warning("아디다스 수집 결과 없음 — 파일 저장 생략 (다음 실행 시 재시도)")
    except Exception as exc:
        logger.warning(f"아디다스 크롤링 실패 (파이프라인은 계속): {exc}")
```
나이키 블록은 이 패턴을 정확히 복제한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.py 에 나이키 크롤링 블록 추가</name>
  <files>main.py</files>
  <action>
1) **import 추가**: `from adidas.crawler import crawl_adidas` 가 있는 줄 바로 다음에 다음 한 줄 추가:
   ```python
   from nike.crawler import crawl_nike
   ```
   import 정렬 순서는 기존 알파벳 그룹(adidas → diff_output 사이)에 nike 가 들어가도록 유지. 즉:
   ```python
   from adidas.crawler import crawl_adidas
   from nike.crawler import crawl_nike
   from diff_output import diff_date_pair, discover_dates
   ```
   (기존 main.py 의 임포트 순서가 adidas → diff_output → kream → naver 임을 따라 nike 는 adidas 와 diff_output 사이 또는 naver 직전에 두되, 알파벳 순서로 일관성 유지: `adidas → diff_output → kream → naver → nike` 순서로 nike 를 맨 마지막 외부 모듈 import 로 둔다.)

   **정확한 결과:**
   ```python
   from adidas.crawler import crawl_adidas
   from diff_output import diff_date_pair, discover_dates
   from kream.comparator import find_arbitrage
   from kream.crawler import DELAY_MAX_SEC, DELAY_MIN_SEC, init_kream_page, search_kream, _human_wait
   from naver.crawler import crawl_naver
   from nike.crawler import crawl_nike
   ```

2) **STEP 1 에 나이키 블록 추가**: 기존 "아디다스 크롤링" 블록(`adidas_output = output_dir / "adidas_products.json"` 으로 시작하고 `except Exception as exc: logger.warning(f"아디다스 크롤링 실패 ...")` 로 끝나는 블록) **바로 다음**, 그리고 `if mode == "crawl":` 블록 **이전**에 다음 코드를 삽입:

   ```python
           # 나이키 크롤링
           nike_output = output_dir / "nike_products.json"
           if nike_output.exists():
               logger.info(f"오늘자 파일 존재 — 나이키 크롤링 스킵: {nike_output}")
           else:
               logger.info("나이키 세일 신발 크롤링 시작")
               try:
                   nike_products = await crawl_nike()
                   if nike_products:
                       _save_json(nike_products, nike_output)
                       logger.info(f"나이키 상품 {len(nike_products)}개 저장 → {nike_output}")
                   else:
                       logger.warning("나이키 수집 결과 없음 — 파일 저장 생략 (다음 실행 시 재시도)")
               except Exception as exc:
                   logger.warning(f"나이키 크롤링 실패 (파이프라인은 계속): {exc}")
   ```

   - 들여쓰기는 아디다스 블록과 동일 (4 space 들여쓰기 × 2 = 8 space, `if mode in ("crawl", "full"):` 블록 안)
   - `try/except` 로 감싸 파이프라인 중단 방지 (아디다스와 동일 패턴)
   - 빈 결과 시 파일 저장 생략 → 다음 실행 시 재시도

3) **STEP 2~4 코드는 절대 수정하지 말 것**:
   - `_load_all_products(output_dir)` 는 `output_dir.glob("*_products.json")` 으로 동작하므로 `nike_products.json` 을 자동 감지한다 (코드 변경 불필요)
   - `find_arbitrage(all_products, kream_products_map)` 는 site_name 무관하게 동작 (코드 변경 불필요)
   - `diff_date_pair` 는 `*_products.json` 전체를 비교하고 `DEFAULT_PRODUCTS_KEY` 를 자동 적용 (코드 변경 불필요)

4) **금지**:
   - 다른 함수(`_save_json`, `_load_all_products`, `_extract_models_from_diff`, `_search_with_page_pool`, `main`, `parse_args`)는 절대 수정하지 말 것
   - STEP 2/3/4 코드 수정 금지
   - 기존 import 순서나 다른 import 라인 수정 금지
   - 기존 로그 메시지 변경 금지
  </action>
  <verify>
    <automated>PYTHONPATH=. venv/bin/python -c "
import ast, pathlib
src = pathlib.Path('main.py').read_text(encoding='utf-8')

# 1) import 추가 확인
assert 'from nike.crawler import crawl_nike' in src, 'nike import 누락'

# 2) 나이키 블록 핵심 라인 확인
assert 'nike_output = output_dir / \"nike_products.json\"' in src, 'nike_output 라인 누락'
assert 'await crawl_nike()' in src, 'crawl_nike 호출 누락'
assert '_save_json(nike_products, nike_output)' in src, '_save_json 호출 누락'
assert '나이키 크롤링 실패 (파이프라인은 계속)' in src, '예외 처리 메시지 누락'

# 3) try/except 감쌈 확인 (정규식)
import re
m = re.search(r'try:\s*\n\s*nike_products = await crawl_nike\(\)[\s\S]*?except Exception as exc:\s*\n\s*logger\.warning', src)
assert m, '나이키 try/except 패턴 불일치'

# 4) STEP 2/3/4 의 핵심 함수 호출이 그대로 유지됨
assert '_load_all_products(output_dir)' in src
assert 'find_arbitrage(all_products, kream_products_map)' in src
assert 'diff_date_pair(all_dates[-2], all_dates[-1], base_output_dir)' in src

# 5) 기존 아디다스 블록 무손상
assert 'adidas_output = output_dir / \"adidas_products.json\"' in src
assert 'await crawl_adidas()' in src

# 6) Python 문법 정합성
ast.parse(src)

# 7) 임포트 가능
import importlib, sys
sys.path.insert(0, '.')
if 'main' in sys.modules: del sys.modules['main']
import main as m
assert hasattr(m, 'main')

print('OK - main.py 검증 통과')
"</automated>
  </verify>
  <done>
- `main.py` 가 syntax error 없이 import 가능
- `from nike.crawler import crawl_nike` 가 import 섹션에 존재
- 나이키 블록이 아디다스 블록 직후, `if mode == "crawl":` 직전에 삽입됨
- `try/except` 로 감싸져 있고 실패 시 WARNING 로그
- 빈 결과 시 파일 저장 생략 분기 존재
- STEP 2/3/4 코드와 다른 함수들은 무손상
  </done>
</task>

<task type="auto">
  <name>Task 2: Makefile 에 make nike 타겟 추가 + ARCHITECTURE.md / history.md 갱신</name>
  <files>Makefile, ARCHITECTURE.md, history.md</files>
  <action>
### 1) Makefile

기존 Makefile 의 `.PHONY` 라인과 새 타겟을 추가한다.

**변경 1**: `.PHONY` 라인에 `nike` 추가:
```
.PHONY: chrome adidas nike crawl kream full
```

**변경 2**: `adidas` 타겟 다음 (그리고 `crawl` 타겟 이전) 에 `nike` 타겟 추가:
```makefile
## 나이키 단독 크롤링
nike:
	$(PYTHONPATH_ENV) $(PYTHON) nike/crawler.py
```

- 주의: nike 는 chrome CDP 가 필요 없으므로 `chrome` 의존성 **추가 금지** (adidas 와 다르게 prerequisite 없음)
- 들여쓰기는 반드시 **TAB** (Makefile 규칙)
- 기존 chrome/adidas/crawl/kream/full 타겟은 절대 수정 금지

### 2) ARCHITECTURE.md

기존 ARCHITECTURE.md 를 다음과 같이 갱신한다 (전체 재작성 금지, 부분 수정만).

**갱신 1 — "실행 방법" 섹션**: 아래 줄 추가 (`make adidas` 다음 줄):
```
make nike     # 나이키 단독 크롤링 (CDP 불필요)
```

**갱신 2 — "실행 흐름" 다이어그램**: STEP 1 블록의 adidas 줄 다음에 nike 줄 추가:
```
  ├── STEP 1: 전체 사이트 크롤링
  │     ├── naver/crawler.py  → output/YYYYMMDD/naver_products.json
  │     │     각 사이트별 병렬 크롤링 (config.SEARCH_URLS)
  │     ├── adidas/crawler.py → output/YYYYMMDD/adidas_products.json
  │     │     Extra Sale 신발 전체 수집, Kids 카테고리 제외
  │     └── nike/crawler.py   → output/YYYYMMDD/nike_products.json
  │           세일 신발 무한스크롤 수집, Kids/주니어 키워드 제외
  │     * 오늘자 파일이 이미 있으면 스킵
```

**갱신 3 — "디렉토리 구조"**: `adidas/` 항목 다음에 `nike/` 추가:
```
├── adidas/               # 아디다스 공홈 크롤러
│   └── crawler.py        # crawl_adidas() — Extra Sale 신발, NaverProduct 포맷 저장
│
├── nike/                 # 나이키 공홈 크롤러
│   └── crawler.py        # crawl_nike() — 세일 신발 무한스크롤, NaverProduct 포맷 저장
```

**갱신 4 — "주요 모듈 설명"**: `### adidas/crawler.py` 섹션 다음에 `### nike/crawler.py` 섹션 추가:
```
### nike/crawler.py
- `crawl_nike()` — 나이키 코리아 세일 신발 페이지(`config.NIKE_SALE_URL`) 크롤링. CDP 불필요, `create_browser(headless=True)` + `new_stealth_page()` 조합 사용.
- `_find_card_selector(page)` — `.product-card` 등 후보 셀렉터 자동 탐지 (adidas 패턴 동일)
- `_scroll_to_bottom(page, card_selector)` — 무한스크롤, N회 연속 변화 없으면 종료 (naver 패턴 동일)
- `_extract_all_products(page, card_selector)` — JS evaluate 로 카드 일괄 파싱
- 모델명 추출 우선순위: ① 카드 텍스트의 스타일코드 정규식(`[A-Z]{2}\d{4}-\d{3}`) → ② URL 마지막 세그먼트 → ③ 상품명 첫 줄 fallback
- Kids 필터: 성인 전용 URL(`clearance-shoes-3yaepzy7ok`) + 카드 텍스트 키워드("kids", "어린이", "유아", "주니어") 2단계 필터링
- 저장 포맷: `NaverProduct` (site_name="nike") → `output/YYYYMMDD/nike_products.json`
```

**갱신 5 — "config.py" 표**: 기존 표 마지막 행 다음에 추가:
```
| `NIKE_SALE_URL` | nike.com/kr 성인 세일 신발 URL | 나이키 크롤링 대상 |
```

**갱신 6 — "새 크롤링 소스 추가 방법" 섹션**: 변경 없음 (이미 일반화되어 있음). 단, 마지막에 다음 한 줄 추가:
```
**참고:** Phase 1 에서 추가된 `nike/crawler.py` 가 이 패턴의 실제 사례이다.
```

### 3) history.md

`history.md` 가 존재하면 기존 내용 맨 위에, 없으면 새로 생성한다.

**파일이 없을 경우 신규 생성:**
```markdown
# 변경 히스토리

## 2026-04-13
- Phase 1 (나이키 공홈 크롤러 추가) 완료: `nike/crawler.py` 신규 작성, `config.NIKE_SALE_URL` 상수 추가, `main.py` STEP 1 에 나이키 크롤링 블록 추가, `Makefile` 에 `make nike` 타겟 추가. 무한스크롤 + JS 일괄 파싱 + Kids 키워드 필터로 세일 신발을 수집하며 기존 diff·Kream 차익 비교 파이프라인에 자동 통합됨.
```

**파일이 있을 경우**: 파일 첫 줄(`# 변경 히스토리` 등 헤더) 직후에 위 `## 2026-04-13` 블록을 prepend (날짜가 같은 항목이 이미 있으면 그 항목에 한 줄 bullet 만 추가).

**금지**: 기존 history.md 내용 삭제 금지, 다른 날짜 항목 수정 금지.
  </action>
  <verify>
    <automated>venv/bin/python -c "
import pathlib, re

# 1) Makefile 검증
mk = pathlib.Path('Makefile').read_text(encoding='utf-8')
assert '.PHONY:' in mk and 'nike' in mk.split('.PHONY:')[1].split('\n')[0], '.PHONY 에 nike 누락'
assert re.search(r'^nike:\s*$', mk, re.MULTILINE), 'nike: 타겟 누락'
assert 'nike/crawler.py' in mk, 'nike/crawler.py 실행 라인 누락'
# 기존 타겟 무손상
for t in ['chrome:', 'adidas:', 'crawl:', 'kream:', 'full:']:
    assert re.search(r'^' + re.escape(t), mk, re.MULTILINE), f'기존 타겟 {t} 손상'
# nike 가 chrome 의존성을 갖지 않음 (CDP 불필요)
nike_block = re.search(r'^nike:.*?(?=\n\S|\Z)', mk, re.MULTILINE | re.DOTALL)
assert nike_block, 'nike 블록 추출 실패'
assert 'chrome' not in nike_block.group().split('\n')[0], 'nike 는 chrome 의존성 금지'

# 2) ARCHITECTURE.md 검증
arch = pathlib.Path('ARCHITECTURE.md').read_text(encoding='utf-8')
assert 'make nike' in arch, '실행 방법에 make nike 누락'
assert 'nike/crawler.py' in arch, '디렉토리 트리에 nike/crawler.py 누락'
assert 'crawl_nike()' in arch, '모듈 설명에 crawl_nike 누락'
assert 'NIKE_SALE_URL' in arch, 'config 표에 NIKE_SALE_URL 누락'
assert 'nike_products.json' in arch, '실행 흐름에 nike_products.json 누락'

# 3) history.md 검증
hist = pathlib.Path('history.md').read_text(encoding='utf-8')
assert '나이키' in hist, 'history.md 에 나이키 항목 누락'
assert 'Phase 1' in hist or 'phase 1' in hist.lower(), 'Phase 1 표기 누락'

print('OK - Makefile / ARCHITECTURE.md / history.md 검증 통과')
"</automated>
  </verify>
  <done>
- `Makefile` 의 `.PHONY` 에 `nike` 추가됨
- `make nike` 타겟이 존재하고 `$(PYTHON) nike/crawler.py` 실행
- nike 타겟이 chrome 의존성 없음 (CDP 불필요)
- 기존 chrome/adidas/crawl/kream/full 타겟 무손상
- `ARCHITECTURE.md` 에 nike 항목이 6개 위치(실행 방법, 흐름, 디렉토리 트리, 모듈 설명, config 표, 새 소스 추가 방법 참고)에 모두 반영됨
- `history.md` 에 Phase 1 변경 요약 항목 존재
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 사람 검증 — make nike 실행 후 실제 nike.com DOM 동작 확인</name>
  <what-built>
- `nike/crawler.py` 가 nike.com/kr 세일 신발 페이지를 무한스크롤로 크롤링
- main.py STEP 1 에 나이키 블록이 통합되어 `make full` 시 자동 호출
- Makefile 에 `make nike` 단독 실행 타겟 추가

이전 검증은 모두 단위/정적 검증이었음. 실제 nike.com DOM 응답·셀렉터·봇 차단 여부는 사람이 직접 실행해야 확인 가능 (RESEARCH.md 의 셀렉터 신뢰도 = MEDIUM, 봇 차단 우회 가능성 = LOW confidence).
  </what-built>
  <how-to-verify>
1. **단독 크롤링 실행**:
   ```bash
   make nike
   ```
   기대 동작:
   - 콘솔에 "나이키 코리아 세일 신발 페이지 이동 ..." (또는 유사 INFO 로그) 출력
   - 카드 셀렉터 탐지 로그 (`'.product-card'` 등)
   - 무한스크롤 진행 로그 (스크롤 횟수 / 카드 수)
   - "수집: ... | 모델=... | 가격=...원" 로그가 수십~수백 줄
   - 마지막에 "총 N개 저장 → output/YYYYMMDD/nike_products.json"

2. **결과 파일 확인**:
   ```bash
   ls -la output/$(date +%Y%m%d)/nike_products.json
   PYTHONPATH=. venv/bin/python -c "
   import json
   data = json.load(open('output/$(date +%Y%m%d)/nike_products.json'))
   print(f'총 {len(data)}개')
   print(f'site_name 분포:', set(d[\"site_name\"] for d in data))
   print(f'첫 항목:', data[0] if data else None)
   print(f'Kids 키워드 포함 항목 수:', sum(1 for d in data if any(k in d['product_name'].lower() for k in ['kids','어린이','유아','주니어'])))
   "
   ```
   확인 사항:
   - [ ] 파일 존재, JSON 파싱 성공
   - [ ] 모든 항목의 `site_name == "nike"`
   - [ ] 항목 수가 0개가 아님 (수십~수백 개)
   - [ ] Kids 키워드 항목 수 == 0
   - [ ] 첫 항목의 `model_name` 이 비어있지 않음 (스타일코드 또는 한글 상품명)
   - [ ] 첫 항목의 `price` 가 정수, 0 이상
   - [ ] 첫 항목의 `url` 이 nike.com 도메인

3. **전체 파이프라인 검증** (선택, 시간 여유 있을 때):
   ```bash
   make full
   ```
   확인 사항:
   - [ ] STEP 1 에서 네이버·아디다스·나이키 모두 호출됨
   - [ ] `output/YYYYMMDD/` 에 `naver_products.json`, `adidas_products.json`, `nike_products.json` 3개 모두 존재
   - [ ] STEP 2 의 `_load_all_products` 로그에 `nike_products.json 로드 — N개` 출력
   - [ ] STEP 3 의 `arbitrage_results.json` 에 `naver_site == "nike"` 또는 비슷한 표기 항목이 1개 이상 (Kream 거래량 ≥ 100, 가격차 ≥ 10,000원 조건 만족 시)
     - 조건 미달 시 0개일 수 있음 — 그래도 STEP 3 는 정상 종료해야 함
   - [ ] STEP 4 (전체 크롤 날짜인 경우) 의 diff 디렉토리에 `nike_products_diff.json` 존재

4. **차단/실패 시나리오 점검**:
   - 결과가 0개거나 "셀렉터 전부 실패" WARNING 이 뜨면:
     - 로그의 "현재 URL", "제목", "본문 앞 500자" 진단 정보를 캡처
     - RESEARCH.md 의 Open Question 2 권고에 따라 다음 단계 고려:
       - `headless=True` → `headless=False` 로 변경 시도
       - 셀렉터 후보(`CARD_SELECTORS`) 확장
       - 최후 수단: CDP 방식 fallback 검토 (별도 플랜 필요)
   - 이 경우 본 검증은 **FAIL** 로 표시하고 후속 플랜을 제안할 것
  </how-to-verify>
  <resume-signal>
다음 중 하나로 응답:
- "approved" — 모든 체크리스트 통과, 나이키 크롤러 정상 동작 확인
- "issues: {설명}" — 일부 또는 전부 실패 (구체적 증상 + 로그 일부 첨부). 후속 디버깅 또는 추가 플랜 필요.
- "skip" — 환경 문제로 즉시 검증 불가. 추후 본인이 검증 예정.
  </resume-signal>
</task>

</tasks>

<verification>
플랜 전체 검증 (Task 1~3 완료 후):

```bash
# 1) main.py 임포트 정합성
PYTHONPATH=. venv/bin/python -c "
import main
import nike.crawler
assert hasattr(main, 'main')
assert hasattr(nike.crawler, 'crawl_nike')
print('imports OK')
"

# 2) make nike 타겟 존재 확인 (실행은 Task 3 에서)
make -n nike

# 3) ARCHITECTURE.md / history.md 키워드 검증 (Task 2 verify 와 동일)

# 4) Phase 1 success criteria 매핑 (사람 검증으로 완료):
#   ✓ make full 실행 시 nike_products.json 생성됨 (Task 3 step 3)
#   ✓ Kream 차익 비교 결과에 나이키 포함 (Task 3 step 3, 조건 미달 시 0개 가능)
#   ✓ diff 추적에 나이키 자동 포함 (Task 3 step 3, DEFAULT_PRODUCTS_KEY 자동 적용)
#   ✓ Kids·비신발 제외 (Task 3 step 2 의 Kids 키워드 카운트 == 0)
```
</verification>

<success_criteria>
이 플랜이 끝나면 (Phase 1 전체 종료):

1. **SRC-01 충족**: `nike/crawler.py` 가 새로운 쇼핑몰 크롤러로 추가되어 동작 (Task 3 step 1, 2 확인)

2. **SRC-02 충족**: 나이키 결과가 NaverProduct 포맷으로 저장되어 STEP 2~4(diff·Kream 비교) 에 자동 통합 (Task 3 step 3 확인)

3. **Phase 1 success criteria 4개 모두 충족**:
   - [x] `make full` → `output/YYYYMMDD/nike_products.json` 생성
   - [x] Kream 차익 비교에 나이키 포함 (조건 만족 시)
   - [x] diff 추적에 나이키 자동 포함
   - [x] Kids/비신발 제외

4. **문서 갱신**: ARCHITECTURE.md 가 현재 코드 상태를 정확히 반영, history.md 에 Phase 1 변경 기록

5. **회귀 없음**: 기존 네이버/아디다스/Kream 동작 무손상 (main.py STEP 2~4 미수정)
</success_criteria>

<output>
완료 후 `.planning/phases/01-nike-crawler/01-02-SUMMARY.md` 작성:
- main.py 변경 diff 요약 (import 1줄 + STEP 1 블록 ~12줄)
- Makefile 변경 사항 (.PHONY 1줄 + nike 타겟 3줄)
- ARCHITECTURE.md 변경 위치 6개 (실행 방법, 흐름, 트리, 모듈 설명, config 표, 참고)
- history.md 추가 항목
- Task 3 사람 검증 결과:
  - 수집된 상품 개수
  - Kids 필터 동작 여부
  - Kream 차익 비교 통합 결과
  - 발견된 셀렉터/모델명 추출 이슈 (있을 경우)
- 후속 개선 사항(있을 경우):
  - 셀렉터 보강 필요 여부
  - 봇 차단 발생 시 CDP fallback 플랜 필요 여부
  - 모델명 추출 정확도 개선 사항
</output>
